void setup() {
  size(400, 400);
}

void draw() {
  background(255);
  // Replace the hard-coded size (50) with a variable
  ellipse(width/2, height/2, 50, 50);
  // Optional: Make the circle change size and location
}
