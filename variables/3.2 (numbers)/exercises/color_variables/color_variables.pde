// Create variables for the r, g, b values
// Change them in draw() to make the circle change color

void setup() {
  size(400, 400);
}

void draw() {
  background(255);
  fill(0, 0, 0);
  ellipse(width/2, height/2, 100, 100);
}
